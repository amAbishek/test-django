pipeline {
    agent any

    stages {
        stage('test') {
            steps {
                echo 'test World'
            }
        }
      stage('build') {
            steps {
                echo 'build World'
            }
        }
      stage('deploy') {
            steps {
                echo 'deploy World'
            }
        }
    }
}
